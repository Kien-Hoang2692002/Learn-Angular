export interface Milestone {
  id: string;
  date: Date;
  title: string;
  description: string;
  imageUrls?: string[];
  size?: string;
  emotionalTone?: string;  
  specialMoment?: string;  
  gift?: string;  
  location?: string; 
  loveNotes?: string;  
}
