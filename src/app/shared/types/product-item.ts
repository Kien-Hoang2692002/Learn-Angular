export type ProductItem = {
    id: number;
    name: string;
    price: number;
    image: string;
}

export type BlogItem = {
    id?: number;
    title?: string;
    body?: number;
    author?: string;
}